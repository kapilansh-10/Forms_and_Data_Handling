<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Searched User</title>
</head>
<body>
    <h1>Searched User</h1>
    <ul>
        <% if(users) {%>
            <% users.forEach(user => { %>
            <li>
                ID: <%= user.id %> <br>Name: <%= user.firstName %> <%= user.lastName %> <br> Email: <%= user.email %> <br> Age: <%= user.age %> <br> Bio: <%= user.bio %>
            </li>
            <% }) %>
       <% } %>
    </ul>
</body>
</html>